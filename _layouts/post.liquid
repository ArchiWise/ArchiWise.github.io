<ul>
	{% for post in site.posts %}
  		{% if post.categories contains 'session' %} 
    		<li>
      			<a href="{{ post.url }}">{{ post.title }}</a>      
      		<!--	<p>{{ post.excerpt }}</p> -->
		    </li>
		{% endif %}
  	{% endfor %}
</ul>

<a href="/sessions.html"><span class"glyphicon glyphicon-chevron-left"></span> Back to sessions</a>

